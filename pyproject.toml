[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "streamwatch"
version = "0.1.0"
description = "StreamWatch ML pipeline"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
